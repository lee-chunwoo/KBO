<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.test">

	<select id="test" resultType="vo.test">
		SELECT NAME, RANKING
		FROM TEAM
		ORDER BY RANKING
	</select>
	
</mapper>